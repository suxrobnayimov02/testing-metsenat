export const mutations = {
	SET_LIST: (state, list) => {
		state.list = list
	},
	SET_ITEM: (state, item) => {
		state.item = item
	},
	// SET_SPONSORS: (state, sponsors) => {
	// 	state.sponsors = sponsors
	// 	state.sponsors.pagination = {
	// 		total: sponsors.total,
	// 		page: sponsors.current_page,
	// 		limit: sponsors.per_page
	// 	}
	// },

}  